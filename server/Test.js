// Filessystem
const c = require('fs');

// Gebruik internet
const http = require('http');

c.readFile() // kun je file inlezen.

// Bouw een server:
/*
1. maak een map
2. run npm init in de map
Er wordt en package.json gemaakt.

run npm install nodemon --save-dev

Pas package.json aan zodat met npm start  automatisch wordt geupdate.

npm install express 
Express wordt geinstalleerd zodat de server makkelijker te gebruiken is.

